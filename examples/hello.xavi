import sum < "./sum.xavi"
import mult < "./operation/mult.xavi"

fn main():
    let x = mult(5,2)
    let y = sum(14,mult(2,3))
    pt("Hello world", y)
    ptln(" hello new line")
    return sum(x,y)
